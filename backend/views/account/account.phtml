<?php include $header ; ?>

<?php

if ($request->isPost()) {
    $form->setFieldValues($request->getPost());
    if (!$form->isValid()) {
        echo $form; // Has errors
    } else {
        echo 'Valid!';
    }
} else {
    echo $form;
}

?>

<?php include $footer ; ?>